# Production build: copy to .env.production before `npm run build`, or set in CI/hosting UI.
# Must be the public API URL including /api/v1 (no trailing slash).
VITE_API_URL=https://your-backend-host.example.com/api/v1
