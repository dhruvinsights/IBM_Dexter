# ============================================================================
# MVP REQUIREMENTS - Minimal PR Review Bot
# ============================================================================
# This file contains ONLY the essential dependencies needed for the MVP
# PR Review Bot. Install time: ~2 minutes
#
# Install: pip install -r requirements-mvp.txt
# ============================================================================

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# AI & LLM — LangChain 0.3.x required by langchain-db2
ollama>=0.5.3,<1.0.0
langchain>=0.3.13,<0.4.0
langchain-core>=0.3.15,<0.4.0
langchain-community>=0.3.20,<0.4.0

# Vector Store - IBM Db2 via official LangChain integration
langchain-db2==0.1.7
ibm-db>=3.2.6,<4.0.0
ibm-db-sa>=0.4.0

# GitHub Integration
PyGithub==2.1.1

# Database (SQLite via SQLAlchemy)
sqlalchemy==2.0.23

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Configuration & Environment
python-dotenv==1.0.0
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
email-validator>=2.0.0

# HTTP Client
httpx>=0.25.2,<0.28.0

# Utilities
python-dateutil==2.8.2
typing-extensions>=4.0.0

# ============================================================================
# NOT INCLUDED IN MVP (Add later if needed)
# ============================================================================
# - GitLab support (GitHub only for MVP)
# - Celery/Redis (no background tasks yet)
# - Multiple LLM providers (Ollama only)
# - Advanced analytics
# - Testing frameworks (add for development)
# ============================================================================

# Total packages: ~15 core dependencies
# Install time: ~2 minutes
# Disk space: ~500MB